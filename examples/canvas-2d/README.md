# Canvas 2D example

Consumes the **same** `OutlineFoldDoc` object as core + `toHtml`.

```bash
cd ../.. && npm run build
# serve this folder (any static server) so ES modules resolve to ../../dist
```

Click nodes to `toggleFold`. Private flags show in labels; unlock is host-side.
